import Dashboard from './pages/Dashboard';

/**
 * Main Application Component
 * 
 * The GNC Calculations app is structured as a single-page application
 * with the Dashboard as the primary view. Future routing can be added
 * here if additional pages are needed.
 */
function App() {
  return <Dashboard />;
}

export default App;
